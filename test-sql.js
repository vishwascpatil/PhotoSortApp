const fs = require('fs'); const initSqlJs = require('sql.js'); async function run() { const SQL = await initSqlJs(); const filebuffer = fs.readFileSync('C:/Users/vishw/AppData/Roaming/Electron/photovault.db'); const db = new SQL.Database(filebuffer); const res = db.exec('SELECT id, file_path, extracted_text, is_document, document_category FROM photos WHERE file_path LIKE \'%8730a6ac112fbf65733620f2c0de6778%\''); if (res.length > 0) { console.log(res[0].values); } else { console.log('Photo not found'); } } run().catch(console.error);
