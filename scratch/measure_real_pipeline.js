const fs = require('fs');
const path = require('path');
const os = require('os');

async function main() {
  const t0 = Date.now();
  // We can require the compiled main bundle or build a test
  console.log('Starting pipeline timing test...');
}
main();
